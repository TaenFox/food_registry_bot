# Пользовательские функции бота

Этот документ фиксирует пользовательские функции бота в формате:

- что делает пользователь;
- что сохраняется в системе;
- какой результат получает пользователь.

Документ описывает именно пользовательский контракт. Если реализация меняется, но наблюдаемое поведение для пользователя остаётся тем же, документ обновлять не нужно. Если меняется доступный сценарий, входной формат или результат, документ нужно обновить в том же рабочем цикле.

## Доступно сейчас

### Регистрация при первом сообщении

Что делает пользователь:

- отправляет `/start`;
- или просто пишет боту первое сообщение.

Что делает система:

- создаёт пользователя в таблице `users`, если его ещё нет;
- привязывает пользователя к `telegram_user_id`;
- сохраняет `username`, если он есть;
- использует часовой пояс `Europe/Moscow` по умолчанию.

Что получает пользователь:

- текстовое подтверждение, что профиль создан или уже существует;
- reply keyboard с доступной кнопкой воды.

### Проверка доступности бота

Что делает пользователь:

- отправляет `/health`.

Что делает система:

- не пишет данные в БД;
- возвращает простой технический ответ.

Что получает пользователь:

- сообщение `ok`.

### Быстрая запись воды кнопкой

Что делает пользователь:

- нажимает кнопку `Вода 250 мл`.

Что делает система:

- создаёт `entry` типа `water`;
- создаёт один `entry_item`:
  - `name = water`
  - `quantity = 250`
  - `unit = ml`
- в `entries.source_text` сохраняет `250 мл`.

Что получает пользователь:

- сообщение `Записал воду: 250 мл.`

### Простая текстовая запись еды

Что делает пользователь:

- отправляет обычное текстовое сообщение, например `гречка с курицей`.

Что делает система:

- создаёт `entry` типа `food`;
- создаёт один `entry_item`, где:
  - `name` равен исходному тексту сообщения;
  - `quantity` и `unit` пока не выделяются;
- сохраняет исходный текст в `entries.source_text`.

Что получает пользователь:

- сообщение `Запись сохранена как еда.`

Ограничения текущего шага:

- сообщение пока не разбирается на несколько блюд;
- количество и единицы измерения из свободного текста пока не извлекаются;
- LLM в этом сценарии пока не используется.

### Нормализованный JSON с несколькими записями

Что делает пользователь:

- отправляет JSON с массивом `entries`;
- в одном сообщении может передать сразу несколько записей, например еду и воду.

Поддерживаемый формат:

```json
{
  "entries": [
    {
      "type": "food",
      "items": [
        {"name": "гречка", "quantity": 200, "unit": "g"},
        {"name": "курица", "quantity": 150, "unit": "g"}
      ]
    },
    {
      "type": "water",
      "items": [
        {"name": "water", "quantity": 250, "unit": "ml"}
      ]
    }
  ]
}
```

Что делает система:

- валидирует JSON;
- для каждого элемента в `entries` создаёт отдельный `entry`;
- для каждого `entry` создаёт связанные `entry_items`;
- для этого сценария не дублирует raw JSON в `entries.source_text`;
- помечает созданные через этот сценарий `entry_items` как `source_type = normalized_json`.

Что получает пользователь:

- подтверждение с количеством созданных записей;
- список сохранённых позиций в формате `название: количество единица`.

Пример ответа:

```text
Сохранил 2 записей из JSON:
- гречка: 200 g
- курица: 150 g
- water: 250 ml
```

Если JSON невалиден:

- запись в БД не создаётся;
- бот отвечает, что ожидает объект вида `{"entries": [...]}` с `type` и `items`.

## Ещё не реализовано

Ниже перечислены важные пользовательские функции, которые ожидаются по продуктовой модели, но пока не доступны:

- разбор свободного текста на несколько блюд или продуктов;
- извлечение количества и единиц измерения из обычного текстового сообщения;
- LLM-разбор пользовательского текста, фото и смешанных сообщений;
- запись тренировок;
- просмотр последних записей прямо из Telegram;
- сводка за день;
- редактирование и удаление сохранённых записей;
- расчёт и показ КБЖУ и других метрик;
- работа с дневными целями.
