# Пользовательские функции бота

Этот документ фиксирует пользовательские функции бота в формате:

- что делает пользователь;
- что сохраняется в системе;
- какой результат получает пользователь.

Документ описывает именно пользовательский контракт. Если реализация меняется, но наблюдаемое поведение для пользователя остаётся тем же, документ обновлять не нужно. Если меняется доступный сценарий, входной формат или результат, документ нужно обновить в том же рабочем цикле.

Для первого релиза в этом документе действует правило:

- если сценарий доступен пользователю сейчас, он описывается в разделе `Доступно сейчас`;
- если сценарий только планируется, он выносится в `WIP` или в список того, что не входит в текущий релиз;
- сценарий редактирования записей в пользовательские обещания текущего релиза не входит.

## Доступно сейчас

### Доступ к боту по разрешению

Что делает пользователь:

- отправляет команду или сообщение боту.

Что делает система:

- проверяет, является ли пользователь админом из конфигурации;
- если нет, проверяет, разрешён ли пользователю доступ;
- если это новый пользователь без разрешения, создаёт для него запись ожидания admin-решения со статусом denied;
- если админ разрешает доступ пользователю без явно заданной категории аккаунта, по умолчанию сохраняет категорию `external`;
- если доступ не разрешён, не выполняет команды и не создаёт записи в БД.

Что получает пользователь:

- если доступ не разрешён, сообщение `Доступ к боту не разрешён.`

### Работа с LLM-провайдером

Что делает пользователь:

- отправляет `/provider`, чтобы посмотреть текущий режим LLM;
- при необходимости отправляет `/provider_save PROVIDER MODEL API_KEY`, чтобы сохранить персональный ключ для конкретной пары `provider + model`;
- если его аккаунт относится к `internal` или он админ, переключает режим между персональным и проектным ключом кнопкой в ответе `/provider`;
- если его аккаунт относится к `external` и рабочего персонального ключа ещё нет, отправляет обычный текстовый запрос и получает подсказку, что сначала нужно сохранить персональный ключ.

Что делает система:

- показывает категорию аккаунта, режим выбора провайдера и сохранённые персональные подключения;
- в ответ `/provider` показывает список доступных источников: проектный ключ и сохранённые персональные подключения;
- для сохранённого персонального подключения открывает отдельный экран с действиями `Выбрать` и `Удалить`;
- для `internal`-аккаунта и админа показывает проектный источник как отдельный выбираемый вариант;
- для `external`-аккаунта не показывает проектный источник;
- хранит персональные `OpenAI`-ключи в БД в зашифрованном виде;
- при сохранении команды `/provider_save` проверяет, что `provider` относится к поддержанным значениям;
- для `Mistral` умеет сохранить и проверить ключ, а затем использовать выбранное подключение в `conversation`, `extraction` и `nutrition` сценариях;
- при сохранении персонального `OpenAI`-ключа сразу пытается проверить доступ к выбранной модели и сохраняет технический статус `valid`, `invalid` или `unknown`;
- использует project provider только для `internal`-аккаунтов;
- позволяет использовать персональные ключи в `conversation`, `extraction` и `nutrition` сценариях;
- если у `external`-пользователя нет рабочего персонального ключа, на произвольный LLM-зависимый запрос подсказывает сначала открыть `/provider` и сохранить ключ;
- в той же подсказке напоминает, что управление сохранёнными подключениями находится в `/provider`;
- если у пользователя нет рабочего LLM-подключения, не блокирует доступ к уже сохранённым данным и аналитике, но честно отказывает в LLM-зависимых сценариях.

Что получает пользователь:

- статус провайдеров и список сохранённых `OpenAI/model` подключений;
- для каждого доступного источника кнопку выбора с отметкой на текущем варианте;
- подтверждение сохранения персонального ключа и результат его технической проверки;
- для сохранённого подключения отдельное меню `Выбрать` / `Удалить`;
- понятное сообщение, если нужный LLM-провайдер сейчас недоступен.

### Пользовательский контекст для LLM

Что делает пользователь:

- отправляет `/context`, чтобы посмотреть текущий сохранённый комментарий для LLM;
- при необходимости отправляет `/context ТЕКСТ`, чтобы сохранить или заменить этот комментарий.

Что делает система:

- хранит пользовательский комментарий в профиле LLM пользователя в зашифрованном виде;
- ограничивает длину комментария `1000` символами;
- показывает текущее сохранённое значение по команде просмотра;
- передаёт этот комментарий в conversational context и в post-entry comment generation;
- не использует этот комментарий как замену фактическим дневным данным, активным диетам или целям.

Что получает пользователь:

- если комментарий ещё не задан, сообщение вида:

```text
Пользовательский контекст для LLM пока не задан.
Чтобы сохранить его, отправь команду:
/context твой текст для ЛЛМ
```

- если комментарий уже сохранён, сообщение с текущим значением;
- если комментарий длиннее `1000` символов, сообщение `Комментарий слишком длинный. Максимум: 1000 символов.`;
- после сохранения подтверждение и тот же актуальный текст комментария.

### Админ-обзор системы

Что делает админ:

- отправляет `/admin`.

Что делает система:

- показывает текущую версию бота;
- показывает текущего админа;
- показывает число админов в конфиге;
- показывает число известных, разрешённых и запрещённых пользователей;
- показывает число `food entries` без полного набора nutrition metrics;
- показывает число `LLM extraction issues` за последние `24` часа;
- показывает число `LLM nutrition issues` за последние `24` часа;
- показывает текущий статус `backfill nutrition`: `idle`, `running`, `completed` или `failed`;
- показывает доступные admin-действия;
- добавляет inline-кнопки `Управление пользователями`, `LLM-ошибки` и `Закрыть`.

Что получает админ:

- сообщение вида:

```text
Панель администратора:
- версия бота: v1
- текущий админ: 468995539
- админов в конфиге: 1
- известных пользователей: 3
- разрешённых пользователей: 1
- запрещённых пользователей: 2
- пользователей с профилем: 1
- food entries без полного набора метрик: 16
- LLM extraction issues за 24ч: 0
- LLM nutrition issues за 24ч: 1
- дозаполнение nutrition metrics: idle

Доступные действия:
- кнопка «Управление пользователями»
- /admin
- /admin_backfill_nutrition [LIMIT]
```

### Управление пользователями из админ-панели

Что делает админ:

- отправляет `/admin`;
- нажимает `Управление пользователями`;
- при необходимости листает страницы кнопками `← Назад` и `Вперёд →`;
- выбирает одного из показанных пользователей кнопкой с его `telegram_user_id`;
- на карточке пользователя либо переключает доступ кнопкой `Разрешить доступ` или `Запретить доступ`, либо переводит аккаунт в категорию `internal` или `external`, либо запускает удаление данных кнопкой `Удалить данные пользователя`;
- при удалении подтверждает действие отдельной кнопкой `Подтвердить удаление` или возвращается кнопкой `Назад`.

Что делает система:

- в списке пользователей показывает не больше `10` пользователей за страницу;
- собирает известных пользователей из профилей `users` и записей доступа `user_access`;
- добавляет в этот же список админов из конфигурации с пометкой `admin`;
- для каждого пользователя показывает текущий статус доступа, категорию аккаунта и наличие профиля;
- при выборе пользователя открывает отдельный экран действий по нему;
- для админа не показывает кнопку переключения доступа, но позволяет открыть экран и удалить его данные;
- при переключении доступа сохраняет или обновляет запись доступа пользователя и сразу обновляет этот же экран;
- при переводе в `internal` или `external` сохраняет категорию аккаунта в `user_access` и сразу обновляет этот же экран;
- перед удалением данных считает, сколько `entries` будет удалено, и не удаляет их сразу;
- удаляет записи только после отдельного явного подтверждения кнопкой;
- даёт кнопки возврата к списку пользователей и закрытия сообщения.

Что получает админ:

- на экране списка сообщение вида:

```text
Пользователи (страница 1, по 10):
- 468995539 [admin; internal; профиля нет]
- 123456789 @alice [доступ разрешён; internal; профиль есть]
- 987654321 @bob [доступ запрещён; unassigned; профиля нет]

Выбери пользователя кнопкой ниже.
```

- на экране конкретного пользователя сообщение вида:

```text
Пользователь:
- Telegram ID: 123456789
- username: @alice
- доступ: разрешён
- категория аккаунта: external
- профиль: есть
- записей в журнале: 42
```

- после переключения доступа callback-ответ `Доступ разрешён.` или `Доступ запрещён.`;
- после смены категории callback-ответ `Категория переключена на internal.` или `Категория переключена на external.`;
- перед удалением данных сообщение-подтверждение с `telegram_user_id` пользователя и количеством записей для удаления;
- после подтверждения сообщение вида `Удалено записей пользователя TELEGRAM_USER_ID: N.`;
- если выбранный пользователь больше недоступен, безопасный alert об этом.

### Уведомление админов о запуске бота

Что делает система:

- при каждом запуске бота отправляет сообщение всем `ADMIN_USER_IDS`;
- в сообщении указывает, что бот запущен, и передаёт версию сборки;
- в качестве версии использует:
  - значение `APP_VERSION`, если оно передано окружением сборки;
  - точный git tag, если текущий коммит помечен тегом;
  - имя текущей ветки, если запуск идёт из рабочей копии;
  - package version или иное fallback-значение, если git недоступен.

Что получает админ:

- служебное сообщение вида:

```text
Бот запущен.
Версия: feature/import-export
```

### Админ-backfill nutrition

Что делает админ:

- отправляет `/admin_backfill_nutrition`;
- при необходимости отправляет `/admin_backfill_nutrition 50`, чтобы изменить лимит за один запуск.

Что делает система:

- находит `food entries`, у которых не хватает одной или нескольких метрик `calories`, `protein`, `fat`, `carbs`, `fiber`;
- берёт до `LIMIT` записей, по умолчанию `20`;
- запускает backfill в фоне и не блокирует обработку других admin-команд;
- повторно запускает nutrition estimation только для этих записей;
- сохраняет или обновляет метрики на уровне `entry_item`.

Пока backfill выполняется, `/admin` показывает статус вида:

```text
- backfill nutrition: running (selected 14, processed 4, remaining 10, skipped 0, failed 0, limit 20)
```

Что получает админ:

- сначала сообщение о старте:

```text
Запускаю backfill nutrition. Лимит: 20.
```

- затем отдельное сообщение по завершении, например:

```text
Backfill nutrition завершён.
Выбрано entries: 3
Обработано entries: 3
Сохранено метрик: 15
```

### Просмотр последних LLM-ошибок админом

Что делает админ:

- отправляет `/admin`;
- нажимает кнопку `LLM-ошибки`;
- при необходимости листает страницы кнопками `← Назад` и `Вперёд →`;
- возвращается в overview кнопкой `К панели`.

Что делает система:

- выбирает последние записи технического журнала LLM-ошибок;
- показывает extraction и nutrition ошибки в одном списке;
- для каждого кейса показывает время, stage, error code, user id, provider/model и сокращённые technical details;
- не требует доступа к серверным stdout/stderr логам.

Что получает админ:

- сообщение вида:

```text
Последние LLM-ошибки. Страница 1, по 5.

- 2026-05-26 10:00:00 UTC | extraction | invalid_payload
  user: 1001
  provider: openai_responses / gpt-5-mini
  details: entries must not be empty
  text: батончик 7 г
  payload: {"entries": []}
```

### Регистрация при первом сообщении

Что делает пользователь:

- отправляет `/start`;
- или просто пишет боту первое сообщение.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- создаёт пользователя в таблице `users`, если его ещё нет;
- привязывает пользователя к `telegram_user_id`;
- сохраняет `username`, если он есть;
- использует часовой пояс `Europe/Moscow` по умолчанию.

Что получает пользователь:

- сообщение с экраном настроек первого уровня;
- постоянную reply keyboard с кнопками `Сегодня`, `Недавние`, `Настройки`, `Отчёт`, `Вода 250 мл`.
- в тексте стартового экрана также подсказки по командам `/provider`, `/context`, `/files`, `/ping`.

### Проверка доступности бота

Что делает пользователь:

- отправляет `/ping`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- не пишет данные в БД;
- возвращает простой технический ответ.

Что получает пользователь:

- сообщение `ok`.

### Загрузка CSV-файла для будущего импорта

Что делает пользователь:

- отправляет в бот `csv`-файл как `document`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- пытается распознать файл как поддержанный обменный `csv` с данными еды, воды или тренировок по заголовкам файла, а не по имени файла;
- допускает как полный файл, так и неполный файл с минимальным обязательным набором полей;
- если файл не подходит, не сохраняет новый файловый артефакт;
- если файл является точным дублем уже загруженного импортного файла, не сохраняет новый файловый артефакт;
- если файл содержит строку, которая дублирует уже существующие данные пользователя, не сохраняет новый файловый артефакт и указывает первую проблемную строку;
- если лимит импортных файлов уже достигнут, не сохраняет новый файловый артефакт;
- если файл подходит, сохраняет локальную копию и создаёт запись подготовленного импортного файла со статусом `готов`.

Что получает пользователь:

- при успешной проверке полного файла сообщение, что файл принят и подготовлен к импорту, с краткой сводкой:
  - распознан тип файла `еда и вода`;
  - сколько будет создано записей еды;
  - сколько будет создано записей воды;
  - диапазон дат;
  - статус `готов`;
- при успешной проверке неполного файла сообщение, что файл принят и подготовлен к импорту:
  - распознан тип файла `еда и вода (неполный файл)`;
  - после импорта часть итогов может быть неполной;
- при успешной проверке workout-файла сообщение, что файл принят и подготовлен к импорту:
  - распознан тип файла `тренировки`;
  - число тренировок;
  - диапазон дат;
- при неподдержанном формате сообщение, что бот не знает, как обработать этот `csv`;
- при точном дубле сообщение `Такой файл уже был загружен ранее.`;
- при дубле данных сообщение с первой проблемной строкой;
- при превышении лимита сообщение с просьбой удалить лишний файл через `/files`.

Формат данных, который поддерживается сейчас:

- файл должен быть `csv`;
- файл должен содержать строку заголовков;
- для полного пользовательского импорта файл использует следующий набор колонок:
  - `Дата`
  - `Приём пищи`
  - `Блюдо / продукт`
  - `Категории`
  - `Количество`
  - `Единица`
  - `Ккал`
  - `Белки, г`
  - `Жиры, г`
  - `Углеводы, г`
  - `Клетчатка, г`
  - `Комментарий`
  - `Источник оценки`
  - `Уверенность оценки`
- для ослабленного варианта обязательны только:
  - `Дата`
  - `Блюдо / продукт`
  - `Количество`
  - `Единица`
- в ослабленном варианте остальные колонки из полного контракта остаются опциональными;
- для еды поле `Единица` не ограничено жёстким справочником:
  - бот принимает стандартные и нестандартные текстовые значения вроде `г`, `мл`, `банка`, `порция`, `порции`;
  - поле `Количество` при этом должно содержать число;
- если в food-строке уже заполнены метрики `Ккал`, `Белки, г`, `Жиры, г`, `Углеводы, г`, `Клетчатка, г`, нестандартная единица сама по себе не мешает импорту;
- если файл неполный и метрики планируется дозаполнять позже, нестандартные единицы вроде `банка` или `порция` могут ухудшать качество последующего дозаполнения по сравнению с `г` и `мл`;
- для workout-импорта поддерживается отдельный `csv` с колонками:
  - `Дата`
  - `Тип`
  - `Длительность, мин`
  - `Ккал тренировки`
- в workout-файле опционально поддерживаются:
  - `Интенсивность`
  - `Ккал учитывать в питании`
  - `Учитывать`
  - `Комментарий`
- workout-импорт создаёт `workout entries` с продолжительностью в `min`, `workout_calories` и `workout_calorie_credit`;
- для воды применяется отдельная нормализация:
  - если строка распознана как вода, единица сохраняется как `ml`, а позиция сохраняется как `water`;
- экспорт еды и воды создаётся в полном food/water-контракте;
- если у пользователя есть тренировки, бот дополнительно подготавливает отдельный workout-`csv` в workout-контракте.

### Список файлов импорта и экспорта

Что делает пользователь:

- отправляет `/files`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- показывает общий список импортных и экспортных файлов пользователя;
- сортирует список так, чтобы необработанные файлы были выше обработанных;
- в пределах группы показывает сначала импортные файлы, затем экспортные;
- для каждого файла показывает его `id`, чтобы на него можно было сослаться при обращении к администратору;
- для каждого файла показывает короткий статус `готов`, `обработан` или `ошибка`;
- для импортного файла показывает распознанный тип файла;
- для неполного импортного файла дополнительно показывает, что после импорта часть итогов может быть неполной;
- для workout-файла показывает число тренировок;
- даёт кнопки действий:
  - создать экспорт;
  - импортировать подходящий импортный файл;
  - скачать экспортный файл;
  - удалить файл;
  - обновить список;
  - закрыть сообщение со списком файлов.

Что получает пользователь:

- сообщение со списком файлов или сообщение, что файлов пока нет;
- inline-кнопки для действий над файлами.

### Явный запуск импорта из подготовленного файла

Что делает пользователь:

- открывает `/files`;
- выбирает импортный файл со статусом `готов` или `ошибка`;
- нажимает кнопку импорта.

Что делает система:

- читает локальную копию ранее сохранённого файла;
- повторно проверяет, не появились ли дублирующие данные;
- если импорт проходит успешно, создаёт записи еды и воды в БД и помечает файл как `обработан`;
- если во время запуска импорта обнаруживается конфликт по данным, помечает файл как `ошибка`.

Что получает пользователь:

- при успешном food/water-импорте сообщение с числом созданных записей еды и воды и указанием, что файл помечен как обработанный;
- при успешном workout-импорте сообщение с числом созданных тренировок и указанием, что файл помечен как обработанный;
- при неуспешном импорте сообщение с причиной ошибки;
- для неполного файла пользователь после импорта может увидеть неполные дневные итоги и при необходимости попросить администратора запустить уже существующий `/admin_backfill_nutrition`;
- если файл уже был импортирован ранее, сообщение, что повторный импорт запрещён.

### Создание и получение экспорта

Что делает пользователь:

- открывает `/files`;
- нажимает кнопку создания экспорта;
- затем скачивает один или несколько подготовленных экспортных файлов из того же списка.

Что делает система:

- собирает текущие данные еды и воды пользователя в поддержанный обменный `csv`;
- если у пользователя есть тренировки, дополнительно собирает их в отдельный workout-`csv`;
- сохраняет локальные копии подготовленных экспортных файлов;
- создаёт одну или несколько записей экспортных файлов со статусом `готов`;
- при скачивании отправляет файл в Telegram и помечает его как `обработан`;
- если лимит экспортных файлов уже достигнут, не создаёт новый экспортный файл.

Что получает пользователь:

- после создания экспорта сообщение или callback-ответ, что файл или файлы готовы к скачиванию;
- после скачивания соответствующий `csv`-файл в Telegram;
- при превышении лимита сообщение с просьбой сначала удалить лишний экспортный файл через `/files`.

### Дозаполнение метрик после импорта неполного файла

Что делает пользователь:

- загружает неполный `csv`;
- сам запускает его импорт через `/files`;
- если после этого видит неполные итоги дня, просит администратора запустить дозаполнение nutrition metrics.

Что делает администратор:

- использует уже существующую команду `/admin_backfill_nutrition`.

Что делает система:

- после импорта неполного файла может обнаружить `food entries` без полного набора nutrition metrics;
- existing backfill выбирает такие записи и дозаполняет им недостающие значения;
- записи с уже полным набором метрик не требуют отдельной обработки.

Что получает администратор:

- тот же отчёт, который уже предусмотрен для `/admin_backfill_nutrition`.

### Просмотр последних записей

Что делает пользователь:

- отправляет `/recent` или `/recent COUNT`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- если указан `COUNT`, использует его как размер страницы, иначе берёт значение по умолчанию `5`;
- принимает для `COUNT` только целое число от `1` до `60`;
- находит последние записи текущего пользователя страницами;
- возвращает их в порядке от новых к старым;
- группирует выводимые записи по пищевым дням пользователя без агрегации метрик;
- показывает локальное время записи и сохранённые позиции записи в человекочитаемом виде;
- если у записи уже есть diet metrics, дополнительно показывает средний diet score по записи;
- если записей больше размера страницы, добавляет inline-кнопки перехода между страницами;
- если на текущей странице есть записи, добавляет inline-кнопку `Открыть запись`.

Что получает пользователь:

- если записи есть, сообщение вида:

```text
Последние записи (страница 1, по 5):

19.05.2026
1. 09:10 — вода (250 мл)
2. 08:30 — яблоко · diet 9.0/10
```

- если записей пока нет, сообщение `Пока записей нет. Отправь еду текстом, фото блюда или нажми кнопку воды.`
- если `COUNT` некорректен, сообщение `Использование: /recent [COUNT], где COUNT от 1 до 60.`

### Открытие записей в recent

Что делает пользователь:

- отправляет `/recent`;
- нажимает `Открыть запись`;
- при необходимости листает страницы кнопками `← Назад` и `Вперёд →`;
- выбирает одну из записей кнопкой вида `08:30 · яблоко` или `09:10 · вода (250 мл)`;
- для food-записи при необходимости открывает конкретное блюдо внутри записи;
- при необходимости возвращается назад к записи, к списку записей или к общему списку recent.

Что делает система:

- показывает отдельный список всех записей на текущей странице recent;
- позволяет открыть конкретную запись и увидеть её detail-экран;
- для записи еды показывает средний балл по включённым у пользователя диетам, суммарные значения по всей записи только для включённых в summary метрик, отдельные блюда, кнопку `Повторить запись целиком`, `Удалить запись`, возврат к списку записей и закрытие сообщения;
- для записи воды и тренировки показывает экран записи, где сейчас доступно только удаление целиком;
- если у water entry уже есть diet metric, может показать и средний балл по диетам на entry-level экране;
- на экране блюда показывает действия для этого блюда и возврат к записи, но не показывает item-level diet score.

Что получает пользователь:

- список записей вида:

```text
Последние записи (страница 1, по 5):

18.05.2026
1. 14:00 — вода (250 мл)
2. 13:00 — яблоко

Выбери запись.
```

- экран записи еды вида:

```text
Запись еды:

13:00 — рис (150 г), курица (120 г)

Средний балл по диетам: 9.0/10

По всей записи:
- калории: 420.0 ккал
- белки: 24.0 г
- жиры: 8.0 г
- углеводы: 51.0 г

Блюда:
1. рис (150 г)
2. курица (120 г)

Выбери блюдо.
```

### Удаление записи целиком из recent

Что делает пользователь:

- открывает нужную запись через `Открыть запись`;
- на экране записи нажимает `Удалить запись`;
- подтверждает удаление кнопкой `Подтвердить` или отменяет его кнопкой `Отмена`;
- при необходимости нажимает `Закрыть`, чтобы удалить текущее inline-сообщение.

Что делает система:

- не удаляет запись сразу, а сначала показывает экран подтверждения;
- удаляет запись целиком как `entry`, а не отдельную позицию внутри записи;
- для записи еды отдельно поясняет, что удалятся все блюда внутри записи;
- после подтверждения обновляет текущее сообщение актуальным списком `/recent`;
- если кнопка устарела или запись уже удалена, не меняет данные и показывает безопасную ошибку.

Что получает пользователь:

- на этапе подтверждения сообщение вида:

```text
Удалить эту запись целиком?

08:30 — яблоко, йогурт

Удалятся все блюда внутри записи.
```

- после успешного удаления обновлённый список recent без удалённой записи, например:

```text
Последние записи (страница 1, по 5):
1. 09:10 — вода (250 мл)
2. 07:45 — кофе
```

- если запись уже удалена или недоступна, сообщение `Запись уже удалена или недоступна.`

### Удаление отдельного блюда из записи recent

Что делает пользователь:

- открывает food-запись и затем конкретное блюдо;
- нажимает `Удалить блюдо`;
- подтверждает удаление кнопкой `Подтвердить` или отменяет его;
- при необходимости закрывает сообщение кнопкой `Закрыть`.

Что делает система:

- не удаляет блюдо сразу, а сначала показывает подтверждение;
- удаляет только выбранный `EntryItem` и связанные с ним `EntryItemMetric`;
- если в записи остаются другие блюда, показывает обновлённую запись;
- если удалено последнее блюдо, удаляет всю `Entry` и возвращает пользователя к списку food-записей на текущей странице.

Что получает пользователь:

- подтверждение вида:

```text
Удалить это блюдо из записи?

рис (150 г)

Если это последнее блюдо в записи, удалится вся запись.
```

- после удаления либо обновлённую запись еды, либо сообщение вида `На странице 1 по 5 сейчас нет записей еды.`

### Повтор блюда и записи из recent

Что делает пользователь:

- открывает food-запись и затем конкретное блюдо;
- нажимает `Повторить сейчас`, если нужно повторить только одно блюдо;
- либо на экране записи еды нажимает `Повторить запись целиком`, если нужно повторить весь приём пищи.

Что делает система:

- при повторе блюда создаёт новую `food entry` с текущим временем и одним выбранным блюдом;
- при повторе записи целиком создаёт новую `food entry` с текущим временем и копией всех блюд исходной записи;
- если у исходных блюд уже есть сохранённые nutrition metrics, копирует их без повторного расчёта;
- если у исходных блюд метрик нет, создаёт новую запись без них;
- после повтора обновляет текущее сообщение актуальным списком `/recent`, где новая запись оказывается сверху.

Что получает пользователь:

- обновлённый список recent с новой записью сверху;
- callback-ответ `Блюдо сохранено как новая запись.` или `Запись сохранена как новый приём пищи.`

### Изменение порции блюда из recent

Что делает пользователь:

- открывает конкретное блюдо внутри food-записи;
- видит отдельное сообщение по этому блюду со своей кнопкой `Закрыть`;
- если для блюда доступна правка порции, нажимает `-10 г` / `+10 г` или `-10 мл` / `+10 мл`.
- если для блюда доступна правка КБЖУ на `100 г`, нажимает кнопки `-25/+25 ккал/100г` или `-5/+5` для белков, жиров и углеводов.

Что делает система:

- показывает кнопки изменения порции только для блюд, у которых есть `quantity`, единица `g` или `ml`, и уже сохранённые nutrition metrics;
- для экрана блюда показывает два отдельных блока значений: `за сохранённую порцию` и `на 100 г`;
- в блоке `на 100 г` показывает только `калории`, `белки`, `жиры` и `углеводы`;
- кнопки КБЖУ на `100 г` доступны только для блюд с весом в `g` и уже сохранёнными значениями этих метрик;
- меняет порцию только у выбранного блюда внутри существующей записи;
- при изменении порции пропорционально масштабирует уже сохранённые nutrition metrics этого блюда, зависящие от веса порции;
- при изменении КБЖУ на `100 г` пересчитывает и сохраняет новое значение `за текущую порцию` для выбранной метрики;
- не использует для такой правки LLM и не запускает повторный nutrition flow;
- если уменьшение привело бы порцию к нулю или ниже, отклоняет действие без изменения записи;
- если уменьшение КБЖУ на `100 г` сделало бы значение отрицательным, отклоняет действие без изменения записи.

Что получает пользователь:

- обновлённый экран блюда вида:

```text
Блюдо:

13:00 — рис (160 г)

За сохранённую порцию:
- калории: 213.3 ккал
- белки: 4.8 г

На 100 г:
- калории: 133.3 ккал
- белки: 3 г

Кнопки КБЖУ ниже меняют блок "На 100 г".
После нажатия бот пересчитывает сохранённые значения за текущую порцию.

Кнопки порции ниже меняют вес блюда и пересчитывают сохранённые значения.
```

- callback-ответ `Порция обновлена.`
- callback-ответ `КБЖУ на 100 г обновлены.`
- если уменьшение недопустимо, сообщение `Порцию нельзя уменьшить дальше.`

### Дневная nutrition summary за сегодня

Что делает пользователь:

- отправляет `/today`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- определяет локальную дату пользователя по его `timezone` и его настройке начала nutrition-day;
- выбирает `food entries` пользователя, у которых `occurred_at` попадает в текущий локальный nutrition-day `HH:00 -> HH:00`;
- суммирует сохранённые nutrition metrics по еде;
- отдельно выбирает `water entries` пользователя в тех же границах пищевого дня и суммирует воду в миллилитрах;
- если у пользователя включены тренировки, отдельно читает workout entries текущего nutrition-day;
- если у workout entries есть `workout_calorie_credit`, добавляет его только к calorie target дня, не меняя protein/fat/carbs/fiber goals;
- в пользовательском ответе показывает только те показатели, которые включены у пользователя в summary settings;
- выбирает формат вывода summary по пользовательской настройке режима отображения;
- если у пользователя включена настройка прогресса дня и выбран режим `бары`, первой строкой показывает progress текущего пищевого дня от его начала до конца;
- строки с белками, жирами, углеводами, клетчаткой и водой начинаются с сокращений `Б:`, `Ж:`, `У:`, `Кл:`, `В:`;
- сам факт тренировки и её calories показываются отдельным workout block;
- если в текущем дне есть `food entries` без полного набора этих метрик, исключает их из итогов и помечает, что summary неполная.
- если в текущем дне есть `water entries` с неподдерживаемым форматом, исключает их из итогов воды и помечает, что итог воды неполный.
- если в текущем дне у записей еды или воды уже есть diet metrics, дополнительно строит отдельный diet block со взвешенным по количеству средним баллом по каждой поддержанной диете за текущий день.

Что получает пользователь:

- если за текущий пищевой день нет ни записей еды, ни записей воды, сообщение `Сегодня пока нет сохранённых записей.`
- иначе сообщение вида:

```text
К: 540.0 / 1800 ккал
Б: 25.3 / 90 г
Ж: 18.1 / 60 г
У: 61.4 / 210 г
Кл: 14.8 / 25 г
В: 750.0 / 2000 мл
```

- если в текущем дне есть diet-оценки, дополнительно показывает блок вида:

```text
Диеты за день:
- низкопуриновая: 9.0/10
```

- если пользователь выбрал режим `бары`, тот же блок показывается bar-line формата вида:

```text
Диеты за день:
Н.пур  [█████████░] 90.0%
```

- если для текущего пищевого дня уже есть goal snapshot, включённые показатели показывают факт и цель:

```text
К: 540.0 / 1800 ккал
Б: 25.3 / 90 г
Ж: 18.1 / 60 г
У: 61.4 / 210 г
В: 750.0 / 2000 мл
```

- если для текущего пищевого дня уже есть goal snapshot и пользователь выбрал режим `бары`, включённые показатели показываются progress bar'ами, например:

```text
Ккал   [███░░░░░░░] 30.0% 540.0/1800 ккал
Б      [██░░░░░░░░] 28.1% 25.3/90 г
Ж      [███░░░░░░░] 30.2% 18.1/60 г
У      [██░░░░░░░░] 29.2% 61.4/210 г
Кл     [█████░░░░░] 59.2% 14.8/25 г
В      [███░░░░░░░] 37.5% 750.0/2000 мл
```

- если дополнительно включена настройка прогресса дня, в режиме `бары` первой строкой появляется шкала вида:

```text
День   [█████░░░░░] 58.3%
```

- если часть food entries за день исключена из-за неполных метрик, дополнительно сообщение содержит пометку вида:

```text
Есть записей еды без полного набора метрик: 1. Итог дня пока неполный.
```

- если часть water entries за день исключена из-за неподдерживаемого формата, дополнительно сообщение содержит пометку вида:

```text
Есть записей воды с неподдерживаемым форматом: 1. Итог воды пока неполный.
```

- если пользователь выключил показ калорий в summary settings, сообщение `В summary сейчас нет включённых показателей.`
- если в текущем дне есть тренировки, после nutrition summary показывается блок вида:

```text
Тренировки:
- 11:00 — бег (40 мин, 757.0 ккал)
```

### Отчёт за период

Что делает пользователь:

- отправляет `/report`;
- при необходимости нажимает inline-кнопку `Следующий период: ...`, чтобы циклически переключать периоды `8`, `16` и `32` дня;
- при необходимости нажимает `Динамика`, чтобы открыть отдельное сообщение с динамикой по подпериодам;
- при необходимости нажимает `Заметные записи пищи`, чтобы открыть отдельное сообщение со списком заметных food entries;
- при необходимости нажимает `Закрыть`, чтобы удалить сообщение отчёта.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- определяет текущий локальный nutrition-day пользователя по его `timezone` и его настройке начала nutrition-day;
- строит отчёт за один из предустановленных периодов:
  - по умолчанию за `8` дней;
  - затем по кнопке циклически за `16` и `32` дня;
- показывает только те nutrition и water метрики, которые включены у пользователя в summary settings;
- для еды считает средние только по тем дням периода, где есть хотя бы одна запись еды;
- для воды считает среднее только по тем дням периода, где есть хотя бы одна запись воды;
- отдельно считает средний diet score по каждой поддержанной диете только по тем `entry_item`, у которых уже сохранена нужная diet metric;
- при расчёте среднего использует вес `quantity`, если он сохранён у item; если quantity нет, использует вес `1`;
- для каждой диеты показывает число оценённых позиций и число дней, в которых были diet-оценки;
- если в периоде есть старые записи еды или воды без нужной diet-оценки, не включает их в среднее и добавляет общую пометку о неполных diet-данных;
- если в дне есть `food entries` без полного набора nutrition metrics, считает такой день днём с данными, но помечает его как неполный;
- для блока `Цели` использует day goals из historical `daily_goal_snapshots`;
- считает день выполненным по включённой метрике, если факт попадает в диапазон вокруг цели по пользовательской настройке `допуск к цели`;
- если у пользователя включены тренировки и за период есть workout entries, показывает отдельный краткий workout block;
- по кнопке `Динамика` строит отдельное сообщение по одной метрике за раз;
- для `Динамики` использует только включённые у пользователя метрики, по которым в периоде есть данные;
- делит текущий период на подпериоды по `4` дня и показывает их в хронологическом порядке;
- для подпериода с данными показывает среднее значение и число дней с данными в формате `(N/4 дней)`;
- если по текущей метрике в подпериоде данных нет, явно пишет `нет данных`;
- в сообщении `Динамика` даёт кнопки `Следующая метрика` и `Закрыть`;
- по кнопке `Заметные записи пищи` строит отдельное сообщение по одной метрике за раз;
- для `Заметных записей пищи` использует только включённые у пользователя метрики, по которым в периоде есть данные;
- показывает список food entries из верхней части распределения по текущей метрике за период;
- использует для отбора текущую пользовательскую настройку `порог заметных записей`;
- показывает не больше `10` записей;
- в сообщении `Заметные записи пищи` даёт кнопки `Следующая метрика` и `Закрыть`;
- по кнопке `Закрыть` удаляет сообщение с отчётом.

Что получает пользователь:

- если все summary-показатели выключены и нет данных по тренировкам, сообщение `В summary сейчас всё скрыто. Включи хотя бы один показатель в /settings.`
- если по включённым показателям нет данных за выбранный период, сообщение `За этот период пока нет данных по включённым показателям.`
- иначе сообщение вида:

```text
Отчёт: 18.05.2026-25.05.2026
Дней в периоде: 8
Дней с данными по еде: 3
Дней с данными по воде: 2

Среднее по дням с данными
- калории: 266.7 ккал
- белки: 26.7 г
- жиры: 10.0 г
- углеводы: 33.3 г
- клетчатка: 4.0 г
- вода: 1000.0 мл

Цели считаются с допуском 10%.
- калории: 0 из 2 дней
- белки: 0 из 2 дней
- жиры: 0 из 2 дней
- углеводы: 0 из 2 дней
- клетчатка: 0 из 2 дней
- вода: 0 из 2 дней

Неполных дней по еде: 1.
```

- если в периоде есть diet-оценки, дополнительно показывает блок вида:

```text
Диеты:
- низкопуриновая: 8.0/10, оценённых позиций: 3, дней с оценкой: 2
```

- если в периоде есть записи без diet score, дополнительно показывает пометку вида:

```text
Диетический балл считается только по позициям с оценкой. Часть записей периода без диетической оценки исключена.
```

- если в периоде есть тренировки, дополнительно блок вида:

```text
Тренировки
- тренировок: 2
```

- inline-кнопки:
  - `Следующий период: 16 дней` или другой следующий период по циклу;
  - `Динамика`;
  - `Заметные записи пищи`;
  - `Закрыть`

Что получает пользователь по кнопке `Динамика`:

- отдельное сообщение вида:

```text
Динамика: 18.05.2026-25.05.2026
Метрика: калории
Доступно: [калории], белки, жиры, углеводы, клетчатка

18.05-21.05: 300.0 ккал (1/4 дней)
22.05-25.05: 500.0 ккал (1/4 дней)
```

- или, если по выбранному периоду нет данных для динамики, alert `За этот период нет данных для динамики.`

Что получает пользователь по кнопке `Заметные записи пищи`:

- отдельное сообщение вида:

```text
Заметные записи пищи: 18.05.2026-25.05.2026
Метрика: калории
Доступно: [калории], белки, жиры, углеводы, клетчатка

- 24.05 · роллы · 950.0 ккал
- 22.05 · бургер · 800.0 ккал
```

- или, если по выбранному периоду нет данных для этой детализации, alert `За этот период нет данных для заметных записей.`

### Настройки и разделы параметров

Что делает пользователь:

- отправляет `/settings`;
- или отправляет `/start`, чтобы сразу открыть этот же экран;
- или нажимает кнопку `Настройки` в постоянном меню;
- выбирает один из разделов `Цели`, `Метрики`, `Отображение`, `Отчёты`, `Диеты`;
- внутри раздела меняет нужную настройку кнопками;
- при необходимости возвращается кнопкой `К разделам`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- находит или создаёт запись пользовательских summary settings;
- находит или создаёт текущие goal preferences;
- читает текущее значение пользовательского флага `workout_logging_enabled`;
- читает список поддержанных диет и текущие пользовательские diet preferences;
- на первом экране кратко объясняет назначение разделов;
- в разделе `Цели` показывает текущие цели и меняет их кнопками с шагом `100 ккал`, `10 г` и `100 мл`;
- в разделе `Метрики` даёт toggles в порядке `Калории`, `Белки`, `Жиры`, `Углеводы`, `Клетчатка`, `Вода`, `Тренировки`;
- в разделе `Отображение` показывает настройки `Прогресс дня`, `Текст/бары`, `Дельта записи`;
- в разделе `Отчёты` показывает настройки `Начало дня`, `Допуск к цели`, `Порог заметных записей`;
- в разделе `Диеты` показывает текущий статус каждой поддержанной диеты;
- при нажатии inline-кнопки меняет соответствующую настройку или статус и обновляет это же сообщение в текущем разделе;
- по кнопке `Закрыть` удаляет сообщение с настройками.

Что получает пользователь:

- на первом экране сообщение вида:

```text
Настройки:
- Цели: дневные цели по калориям, БЖУ, клетчатке и воде.
- Метрики: какие показатели учитывать в итогах дня и отчётах.
- Отображение: формат summary, прогресс дня и дельта после записи.
- Отчёты: начало пищевого дня, допуск к цели и порог заметных записей.
- Диеты: какие диетические оценки учитывать.

Выбери раздел кнопкой ниже.
```

- inline-кнопки первого уровня: `Цели`, `Метрики`, `Отображение`, `Отчёты`, `Диеты`, `Закрыть`;
- в разделе `Цели` сообщение с текущими значениями целей и кнопками `−/+` по каждой метрике;
- в разделе `Метрики` сообщение со статусами `включено/выключено` и кнопками `on/off`;
- в разделе `Отображение` сообщение с текущим режимом `текст/бары`, `прогрессом дня` и `дельтой записи`;
- в разделе `Отчёты` сообщение с текущими значениями `начала дня`, `допуска к цели` и `порога заметных записей`;
- в разделе `Диеты` сообщение со статусом каждой поддержанной диеты.

### Опциональная запись тренировки текстом

Что делает пользователь:

- сначала при необходимости включает тренировки в `/settings`;
- затем отправляет явную запись вроде `тренировка: бег 40 минут` или `сегодня была силовая 50 минут`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- пропускает сообщение через routing;
- если запись тренировок выключена, не сохраняет сообщение и прозрачно предлагает включить функцию в `/settings`;
- если запись тренировок включена, извлекает `workout entry` из текста;
- сохраняет запись как `entry_type = workout`;
- сохраняет исходный текст сообщения в `source_text`;
- если у тренировки известны `workout_calories`, дополнительно сохраняет отдельную metric `workout_calorie_credit`;
- в первой реализации `workout_calorie_credit` считается по правилу: каждые полные `200 ккал` тренировки дают `100 ккал` зачёта в питание;
- calorie credit влияет только на дневную calorie goal / remaining calories и не меняет protein/fat/carbs/fiber goals.

Что получает пользователь:

- короткое подтверждение сохранения вида:

```text
Сохранил:
- бег (40 мин)
```

- если у тренировки есть calories, confirmation может сразу включать обновлённый day-report и workout block.
- у сообщения подтверждения есть inline-кнопка `Подробнее`;
- кнопка `Подробнее` открывает экран записи с действиями в стиле `/recent`;
- кнопка `Закрыть` на этом экране не удаляет сообщение, а возвращает его к пересчитанному confirmation первого уровня.

### Опциональная запись тренировки одним скриншотом

Что делает пользователь:

- сначала при необходимости включает тренировки в `/settings`;
- затем отправляет один скриншот из workout-приложения без необходимости расписывать тренировку текстом.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- пропускает изображение через journal routing;
- если запись тренировок выключена, не сохраняет скриншот и прозрачно предлагает включить функцию в `/settings`;
- если запись тренировок включена, пытается извлечь `workout entry` из содержимого скриншота;
- если пользователь прислал album или несколько изображений одной группой, не пытается разбирать их и просит прислать один основной скриншот;
- при достаточной уверенности сохраняет запись как `entry_type = workout`;
- если на скриншоте явно видны калории тренировки, сохраняет их как factual metric `workout_calories`;
- если сохранены `workout_calories`, дополнительно создаёт отдельную metric `workout_calorie_credit`;
- по умолчанию `workout_calorie_credit` считается по фиксированному правилу: каждые полные `200 ккал` тренировки дают `100 ккал` nutrition credit;
- не сохраняет состав упражнений, мышечные карты, зоны нагрузки и другие подробности screenshot breakdown на этом этапе.

Что получает пользователь:

- короткое подтверждение сохранения вида:

```text
Сохранил:
- тренировка (90 мин)
- калории тренировки: 757.0 ккал
- к компенсации питания: 300.0 ккал

К: 0.0 / 2557 ккал
...

Тренировки:
- 03:00 — тренировка (90 мин, 757.0 ккал, компенсация 300.0 ккал)
```

### Просмотр и настройка дневных nutrition goals

Что делает пользователь:

- отправляет `/goal`, чтобы сразу открыть раздел `Цели`;
- или открывает `/settings` и выбирает раздел `Цели`;
- меняет цели кнопками `−/+` с фиксированным шагом;
- при необходимости возвращается кнопкой `К разделам` или закрывает сообщение.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- открывает тот же раздел `Цели`, что и в `/settings`;
- показывает текущие значения целей по калориям, БЖУ, клетчатке и воде;
- меняет цель только по кнопке конкретной метрики и сразу обновляет это же сообщение;
- не использует slash-аргументы `/goal 1800`, `/goal protein 90` и подобные как пользовательский способ настройки.

Что получает пользователь:

- сообщение вида:

```text
Раздел «Цели»:
Настрой дневные цели кнопками ниже.
Шаг изменения: 100 ккал, 10 г для БЖУ и клетчатки, 100 мл для воды.

- калории: 1800 ккал
- белки: 90 г
- жиры: 60 г
- углеводы: 210 г
- клетчатка: 25 г
- вода: 2000 мл
```
- кнопки `−/+` для каждой метрики и кнопки `К разделам`, `Закрыть`.

### Быстрая запись воды кнопкой

Что делает пользователь:

- нажимает кнопку `Вода 250 мл`.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- создаёт `entry` типа `water`;
- создаёт один `entry_item`:
  - `name = water`
  - `quantity = 250`
  - `unit = ml`
- в `entries.source_text` сохраняет `250 мл`.

Что получает пользователь:

- сообщение вида:

```text
Сохранил:
- вода (250 мл)

К: 0.0 / 1800 ккал
Б: 0.0 / 90 г
Ж: 0.0 / 60 г
У: 0.0 / 210 г
Кл: 0.0 / 25 г
В: 250.0 / 2000 мл (+250.0 мл)
```

- после записи бот показывает тот же day-report, что и `/today`, но для затронутых показателей дополнительно выделяет вклад новой записи;
- у сообщения подтверждения есть inline-кнопка `Подробнее`;
- кнопка `Подробнее` открывает экран этой записи с теми же item-level действиями, что и в `/recent`;
- кнопка `Закрыть` на этом экране возвращает сообщение к пересчитанному confirmation первого уровня;
- если у нового food или water entry появилась diet-оценка, в этом же day-report дополнительно показывает блок `Средний балл пользы по диетам (0 - не полезно, 10 - полезно)` со взвешенным по количеству средним баллом по активным диетам;
- для water entry diet score не запрашивается у LLM: для каждой поддержанной диеты его значение заранее фиксируется в коде;
- если включена настройка `Дельта записи`, в diet block после записи дополнительно показывается изменение среднего балла за день от новой записи, например `(+1.4)` или `(-0.8)`;
- если у пользователя включена настройка `Дельта записи`, в конце затронутых строк показывается суффикс вида `(+250.0 мл)`;
- если настройка `Дельта записи` выключена, суффикс скрывается;
- если у пользователя включён режим `бары`, для воды и других затронутых показателей используется шкала `█/▓/░`, где `▓` показывает объём, добавленный только что сохранённой записью.

### Текстовая или фото-запись еды с обязательными метриками

Что делает пользователь:

- отправляет текст с едой, например `гречка с курицей`;
- или отправляет фото еды, при необходимости с caption.
- при желании может дополнительно указать явные данные с этикетки продукта текстом;
- или прислать фото упаковки/этикетки, если на нём читаются nutrition facts.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- сначала пропускает свободное сообщение через routing boundary между journal, conversation и ambiguous;
- передаёт в extraction layer только сообщения, которые классифицированы как journal;
- передаёт сообщение в extraction layer;
- ожидает нормализованный `entries` payload;
- создаёт отдельные `entry` и `entry_items` из extraction result;
- если в тексте или на фото есть явные nutrition facts этикетки для food item, извлекает их как отдельный factual `nutrition_label`;
- для всех `food` items после сохранения вызывает nutrition estimation;
- передаёт в nutrition layer не только `name`, `quantity` и `unit`, но и структурированные label facts, если они были явно распознаны;
- если label facts достаточно явные, nutrition layer использует их как приоритетную factual основу для КБЖУ;
- сохраняет метрики `calories`, `protein`, `fat`, `carbs`, `fiber` для каждого food item;
- сохраняет уровень доверия для каждой метрики;
- если у пользователя включена хотя бы одна поддержанная диета, после сохранения entry items дополнительно вызывает diet evaluation только для новых food entries;
- для water entries вместо LLM использует фиксированное per-diet значение, заданное в коде;
- сохраняет diet scores как item-level metrics в `entry_item_metrics`, например `low_purine_score`, `insulin_resistance_score` и `gastritis_score`;
- включает список активных диет пользователя в factual context для `nutrition coach`;
- только после успешной записи и еды, и метрик возвращает ответ пользователю.
- если запись относится к еде и для неё доступен conversational LLM, может дополнительно построить короткий `nutrition coach` comment по новой записи, текущему состоянию дня и активным диетам пользователя;
- если такой comment был построен, он сохраняется в `entries.llm_comment` для food entries этого write flow;
- если diet evaluation не удалась, запись всё равно считается успешно сохранённой, а пользователь получает обычный confirmation response без отдельной ошибки;
- если post-entry comment generation не удалась, запись всё равно считается успешно сохранённой, а пользователь получает обычный confirmation response без отдельной ошибки.

Что получает пользователь:

- сообщение вида:

```text
Сохранил:
- гречка с курицей

К: 220.0 / 1800 ккал (+220.0 ккал)
Б: 7.6 / 90 г (+7.6 г)
Ж: 2.2 / 60 г (+2.2 г)
У: 42.8 / 210 г (+42.8 г)
Кл: 5.1 / 25 г (+5.1 г)
В: 0.0 / 2000 мл
```

- confirmation после записи использует тот же итог пищевого дня, что и `/today`;
- у сообщения confirmation есть inline-кнопка `Подробнее`;
- кнопка `Подробнее` открывает экран этой записи с теми же действиями, что и в `/recent`;
- кнопка `Закрыть` на экране записи не удаляет сообщение, а возвращает его к пересчитанному confirmation первого уровня;
- если в текущем дне уже есть diet-оценки, confirmation включает и тот же средний diet score за день, что и `/today`;
- в `bars` mode блок `Средний балл пользы по диетам` в confirmation тоже рендерится bar-line, а не обычным списком;
- в `bars` mode diet block показывает bar и средний балл `x/10`, без процентов;
- после successful food write flow confirmation может дополнительно содержать короткий diet-aware комментарий без отдельного агентского префикса;
- этот комментарий строится по только что сохранённой записи, её delta и обновлённому factual state дня;
- комментарий остаётся коротким, не открывает длинный диалог и не создаёт conversational session;
- если включена настройка `Дельта записи`, в `text` mode у затронутых строк добавляется суффикс `(+delta unit)`;
- если настройка выключена, этот суффикс скрывается;
- в `bars` mode у затронутых строк `█` показывает объём до записи, `▓` - вклад новой записи, `░` - оставшийся объём до цели;
- в `bars` mode у diet block `▓` показывает рост среднего diet score от новой записи, а `▒` - участок, который средний балл потерял из-за новой записи;
- настройка `Дельта записи` управляет только суффиксом в конце строки и не отключает саму delta-подсветку `▓`.

Ограничения текущего шага:

- structured extraction для текста и фото может работать как через dev payload, так и через отдельно включённый LLM provider;
- label facts с упаковки используются только если они явно видны в тексте пользователя или на фото и могут быть структурированы без опасной выдумки;
- длинный или неочевидный свободный текст может быть безопасно отклонён как ambiguous и не будет сохранён молча;
- если extraction provider не смог вернуть валидный structured payload, запись еды не создаётся;
- если nutrition provider вернул невалидный payload, запись еды тоже не создаётся;
- на текущем шаге просит LLM возвращать `item.name` на русском языке, даже если фото отправлено без caption;
- если на фото видно одно цельное блюдо, старается сохранить его одной позицией, а не раскладывать на ингредиенты по догадке;
- если масса неочевидна, nutrition estimate всё равно должна быть сохранена, но с более низким уровнем доверия;
- если LLM вернула бытовую единицу вроде `стакан`, `чашка` или `шт`, extraction сначала пытается перевести её в `ml` или `г` для типовых кейсов;
- если такую единицу нельзя надёжно привести к базовой, оценка всё равно строится по названию блюда без передачи `quantity/unit`;
- поддерживается один photo message за раз;
- используется только самое крупное фото из массива `message.photo`;
- если пользователь прислал photo album или media group, бот не пытается разбирать их и просит прислать одно основное фото;
- несколько фото в одном сообщении как отдельный продуктовый сценарий пока не поддерживаются;
- если extraction provider не умеет обрабатывать фото, сценарий не сработает.
- post-entry comment сейчас добавляется только после food write flow и может быть пропущен для multi-date записи.

### Свободный conversational запрос

Что делает пользователь:

- отправляет обычное текстовое сообщение с вопросом, просьбой о совете или обсуждением питания, воды, режима, целей или тренировок;
- например `Как добрать белок без лишних калорий?`

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- пропускает сообщение через routing boundary;
- если сообщение выглядит как conversational request, не отправляет его в extraction/save flow;
- не создаёт `entry`, `entry_items` и nutrition metrics;
- передаёт текст в `nutrition coach`;
- для ответа всегда собирает factual context текущего пищевого дня пользователя:
  - текущие итоги по калориям, белкам, жирам, углеводам, клетчатке и воде;
  - прогресс против дневных целей;
  - список включённых диет пользователя;
  - список factual workout entries текущего пищевого дня, если у пользователя включена workout-функция;
  - недавние сохранённые записи;
- `nutrition coach` получает workout context дня всегда, а не только по эвристике релевантности вопроса;
- если у пользователя включены диеты, `nutrition coach` учитывает их как пищевые ограничения и в conversational reply, и в post-entry comment;
- `nutrition coach` может учитывать тренировку в расширенном nutrition-смысле, например для белка, углеводов, hydration и meal timing, но не должен выдавать выдуманные workout metrics;
- если у пользователя уже есть активная conversational coach session не старше одного часа, использует её краткий контекст и несколько последних conversational turns;
- если пользователь отвечает reply на предыдущее conversational сообщение `nutrition coach`, бот сначала пытается продолжить именно ту же coach session, даже если часовой TTL уже истёк;
- сохраняет только conversational turns `nutrition coach` в отдельную coach session memory, не смешивая их с journal entries;
- возвращает разговорный ответ отдельным сообщением.

Что получает пользователь:

- ответ `nutrition coach`, учитывающий текущее состояние дня;
- conversational reply приходит как reply на исходное сообщение.

Пример ответа:

```text
Чтобы добрать белок без лишних калорий, сегодня удобнее смотреть на нежирный творог, йогурт без сахара, куриную грудку, рыбу или яичные белки. Если хочешь, могу предложить 2-3 варианта ужина под остаток твоих калорий и белка.
```

Дополнительно на текущем шаге:

- `nutrition coach` умеет отвечать на meal-planning вопросы, вопросы о воде и общие health topics;
- если пользователь перечисляет доступные продукты текстом, бот может предложить, что лучше приготовить с учётом текущих метрик дня;
- `nutrition coach` хранит conversational memory в течение часа после последнего conversational message;
- короткие follow-up сообщения внутри активной coach session, например `это будет сценарий А`, продолжают разговор и не должны молча сохраняться как еда;
- reply на конкретный ответ `nutrition coach` имеет приоритет над обычным поиском активной сессии по TTL;
- если текст такого reply выглядит как явная запись факта, например `запиши кусок хлеба` или `съел яблоко`, сообщение всё равно идёт в factual logging, а не в coach reply;
- пользователь может отправить фото холодильника или доступных продуктов с явным coaching-caption вроде `что лучше приготовить из этого?`;
- такое сообщение идёт в `nutrition coach` вместе с самим изображением и не сохраняется как journal entry;
- фото без caption по-прежнему считается journal photo candidate, чтобы не размывать factual photo logging;
- граница остаётся консервативной: `conversation_photo` включается только при явном conversational cue в caption.

### Неоднозначное свободное сообщение

Что делает пользователь:

- отправляет свободный текст, по которому недостаточно надёжно понять, это запись факта или разговор;
- например длинное сообщение без явного вопроса и без явных признаков записи.

Что делает система:

- сначала проверяет, разрешён ли пользователю доступ;
- пропускает сообщение через routing boundary;
- если классификация недостаточно надёжна, не создаёт запись в БД;
- не отправляет сообщение ни в journal extraction flow, ни в conversational assistant как уже выбранный маршрут;
- возвращает короткий прозрачный ответ с просьбой уточнить намерение.

Что получает пользователь:

- сообщение вида:

```text
Не понял, это запись в дневник или вопрос.
Если хочешь сохранить факт, пришли явную запись еды или воды.
Если хочешь совет или объяснение, задай вопрос прямо.
```

## WIP и вне текущего релиза

Ниже перечислены сценарии, которые могут понадобиться продукту дальше, но в пользовательские обещания первого релиза не входят:

- `WIP`: более сложные mixed-media сценарии вокруг скриншотов тренировок, если для них появится реальная потребность;
- вне текущего релиза: редактирование уже сохранённых записей;
- dev-only сценарий: structured payload для extraction contract.
