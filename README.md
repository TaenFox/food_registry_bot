# food_registry_bot

`food_registry_bot` - Telegram-бот для ведения дневника питания и воды, с отдельным factual-aware conversational слоем `nutrition coach`.

Пользователь отправляет боту текстовое сообщение или фото еды. Система сначала отделяет команды, journal-сообщения и conversational-сообщения. Journal flow с помощью LLM извлекает структуру записи, оценивает КБЖУ и дополнительные метрики, сохраняет результат в базе данных и возвращает краткий текстовый отчёт по записи и текущему дню. После успешной записи еды бот может добавить короткий nutrition-aware комментарий поверх тех же factual данных. Conversational flow работает как `nutrition coach`: отвечает на свободные вопросы отдельно, не сохраняет их как factual entries, всегда опирается на актуальные метрики и recent entries текущего дня, держит короткую session memory около одного часа и поддерживает первый безопасный мультимодальный сценарий для фото продуктов или холодильника с явным coaching-caption.

Проект делается в первую очередь для личного использования, но архитектура должна позволять без существенных изменений дать доступ нескольким знакомым пользователям.

## Что доступно в первом релизе

- принимать текстовые сообщения о еде и воде;
- принимать фото еды и использовать его как вход для оценки состава и параметров;
- выделять из сообщения отдельные блюда или позиции;
- оценивать количество, единицу измерения, КБЖУ и другие поддерживаемые метрики;
- сохранять записи в PostgreSQL;
- возвращать пользователю краткий отчёт по добавленной записи и сводку по дню;
- отвечать на свободные nutrition-вопросы отдельно от journal flow;
- удалять запись целиком из списка последних записей.

## Основные сценарии

### Запись еды

Пользователь пишет сообщение вроде:

`съел паштет с хлебом и выпил кофе`

или отправляет фото блюда.

Система:

1. определяет, какие блюда или продукты были съедены;
2. оценивает объём, единицы измерения и пищевые показатели;
3. сохраняет запись в БД;
4. отправляет краткое подтверждение и обновлённый итог дня.

### Удаление записи

Пользователь может открыть `/recent`, выбрать одну из последних записей и удалить запись целиком. После удаления дневные показатели пересчитываются.

## Что не входит в первый релиз

- редактирование уже сохранённых записей;
- запись тренировок;
- разбор скриншотов тренировок;
- mixed-media сценарии beyond текущего food photo / coaching photo contract.

## Ключевые принципы

- Источник истины только один: PostgreSQL.
- Google Sheets не используются.
- Отчёты формируются в текстовом виде прямо в Telegram.
- Новые метрики добавляются разработчиком в коде, а не задаются пользователем произвольно.
- Пользователь может включать или отключать отслеживание поддержанных метрик в своём профиле.
- Исторические данные не должны изменять смысл при изменении текущих пользовательских настроек.
- Документация должна описывать реальные решения и не содержать процессных секций без практической пользы.

## Архитектурная схема

Систему предполагается строить из нескольких логических слоёв:

### Telegram layer

Отвечает за приём сообщений, фото и команд, а также за отправку пользователю итогового ответа.

### Message routing layer

Отвечает за безопасную границу между:

- command messages;
- journal messages;
- conversational messages;
- ambiguous messages, которые лучше отклонить без записи, чем сохранить неверный факт.

### Nutrition coach layer

Использует factual context дня пользователя и короткую session memory, чтобы:

- отвечать на nutrition / hydration / meal-planning вопросы и общие health topics;
- принимать фото холодильника или доступных продуктов как conversational input, если caption явно просит совет;
- добавлять короткий post-entry nutrition comment после успешной записи еды;
- не вмешиваться в routing и factual contract самого logging flow.

### AI parsing layer

Использует LLM для:

- извлечения структуры входного сообщения;
- оценки КБЖУ и других поддерживаемых метрик;
- подготовки краткого пользовательского комментария.

### Domain layer

Отвечает за нормализацию записей, пересчёт дневных итогов, применение пользовательских настроек и корректировку целей дня.

### Persistence layer

Хранит пользователей, записи, позиции записей, определения метрик, значения метрик и дневные цели.

## Доменная модель

Ниже зафиксирована рабочая модель на уровне предметной области. Детали схемы БД и миграций следует уточнять в ADR.

### Пользователь

Хранит:

- Telegram identity;
- часовой пояс;
- настройки отображения и отслеживания метрик;
- базовые дневные цели.

### Запись

Запись - это событие в дневнике пользователя. В первом релизе поддерживаются записи еды и воды.

Примерные поля:

- пользователь;
- дата и время;
- тип записи;
- тип приёма пищи, если применимо;
- исходный текст сообщения;
- комментарий LLM;
- текущее состояние записи.

### Позиция записи

Одна запись может содержать несколько позиций. Например, в одном сообщении могут быть указаны паштет, хлеб и кофе.

Примерные поля:

- ссылка на запись;
- название блюда или продукта;
- количество;
- единица измерения;
- порядок внутри записи;
- уверенность оценки;
- источник оценки.

### Определение метрики

Справочник поддерживаемых системой метрик.

Примеры:

- `calories`
- `protein`
- `fat`
- `carbs`
- `fiber`
- `purine_risk`

Для метрики должны быть определены:

- код;
- тип значения;
- единица измерения;
- уровень применения: позиция, запись или день;
- системный статус;
- правила агрегации.

### Значение метрики

Хранит рассчитанное значение конкретной метрики для позиции, записи или дня.

Примерные поля:

- ссылка на определение метрики;
- ссылка на запись и/или позицию;
- числовое, текстовое или структурированное значение;
- комментарий или объяснение;
- источник оценки.

### Дневные цели

Дневные цели должны храниться как снимок настроек на конкретную дату.

Это нужно для того, чтобы:

- прошлые дни не пересчитывались при изменении текущих настроек пользователя;
- исторические отчёты оставались интерпретируемыми.

Минимально здесь нужны:

- базовая цель дня;
- эффективная цель дня;
- дата;
- пользователь.

Например, если базовая цель пользователя составляет `1800` ккал, то при создании нового дня это значение фиксируется в дневной записи. Даже если текущие настройки потом изменятся, прошлые дни должны сохранить исходный смысл.

## Поддерживаемые метрики

На старте система должна уметь работать как минимум со следующими метриками:

- калории;
- белки;
- жиры;
- углеводы;
- клетчатка;
- объём воды;
- пуриновый риск.

Список поддерживаемых метрик расширяется разработчиком через код и миграции. Пользователь может выбрать, какие из поддерживаемых метрик показывать и отслеживать в своём контексте.

## Работа с LLM

LLM в проекте не сводится только к классификации текста. Она должна помогать в трёх связанных задачах:

1. извлечение структуры сообщения;
2. оценка нутриентных и дополнительных метрик;
3. ответа `nutrition coach` поверх factual context;
4. генерация краткого factual-ответа пользователю.

При этом система должна хранить разумные допущения, уровень уверенности и источник оценки, особенно в случаях с фото, сложными блюдами и неоднозначным описанием.

## Технологические ориентиры

- язык реализации: Python;
- бот запускается в Docker;
- основная база данных: PostgreSQL;
- развитие схемы БД должно идти через понятные инкрементальные миграции.

## Локальная конфигурация

Секреты для локальной разработки не хранятся в репозитории и не добавляются в `.venv`.

Проект ожидает локальный файл окружения по пути:

`../food_registry_bot_local/.env`

относительно корня репозитория.

Для создания каталога и шаблона файла используй:

```bash
./scripts/init-dev-secrets.sh
```

Скрипт создаёт sibling-каталог `food_registry_bot_local`, добавляет в него `.env` с болванкой переменных и `README.md` с пояснением назначения каталога.

Для admin-команд укажи в локальном `.env` переменную:

```env
ADMIN_USER_IDS=123456789
```

Если админов несколько, перечисли их через запятую.

Минимальный шаблон локального `.env` на текущем этапе выглядит так:

```env
APP_ENV=local
BOT_TOKEN=
ADMIN_USER_IDS=
EXTRACTION_PROVIDER=structured_payload
LLM_MODEL=gpt-5-mini
NUTRITION_MODEL=gpt-5-mini
CONVERSATION_MODEL=gpt-5-mini
OPENAI_API_KEY=

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=food_registry
POSTGRES_USER=food_registry
POSTGRES_PASSWORD=food_registry
```

Для extraction layer на текущем этапе поддерживаются два режима:

- `EXTRACTION_PROVIDER=structured_payload` - дев-режим, где бот принимает уже нормализованный structured payload;
- `EXTRACTION_PROVIDER=llm` - режим для прямой LLM-интеграции через OpenAI Responses API.

Если включён `EXTRACTION_PROVIDER=llm`, приложение использует `OPENAI_API_KEY` и `LLM_MODEL` для создания OpenAI adapter. Сам Telegram handler при этом не меняется: он по-прежнему работает только с extraction service boundary.

На текущем этапе direct LLM extraction поддерживает:

- text-only сообщения;
- одно фото еды с опциональным caption;
- тот же `entries` extraction contract на выходе, что и для structured payload режима.

Для conversational `nutrition coach` на текущем этапе отдельный provider-флаг не нужен:

- если `OPENAI_API_KEY` не задан, conversational routing остаётся доступным, но вместо LLM-ответа бот явно сообщает, что `nutrition coach` не настроен;
- если `OPENAI_API_KEY` задан, бот использует OpenAI Responses API для factual-aware coach reply.

Модели на текущем этапе разделены так:

- `LLM_MODEL` - модель extraction layer;
- `NUTRITION_MODEL` - модель nutrition estimation layer;
- `CONVERSATION_MODEL` - модель `nutrition coach`.

Если не нужен отдельный тюнинг, все три можно оставить одинаковыми.

## Документация

- `README.md` описывает проект на верхнем уровне;
- `docs/user-functions.md` фиксирует пользовательские сценарии и текущий наблюдаемый результат для пользователя;
- `docs/development-workflow.md` фиксирует рабочий процесс и короткий путь релиза;
- архитектурные и модельные решения фиксируются в `docs/adr/`;
- `AGENTS.md` хранит постоянные инструкции по работе с репозиторием.
