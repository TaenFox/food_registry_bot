# food_registry_bot

`food_registry_bot` - Telegram-бот для ведения дневника питания, воды и тренировок, с отдельным factual-aware conversational слоем `nutrition coach`.

Пользователь отправляет боту текстовое сообщение или фото еды. Система сначала отделяет команды, journal-сообщения и conversational-сообщения. Journal flow с помощью LLM извлекает структуру записи, оценивает КБЖУ и дополнительные метрики, сохраняет результат в базе данных и возвращает краткий текстовый отчёт по записи и текущему дню. После успешной записи еды бот может добавить короткий nutrition-aware комментарий поверх тех же factual данных. Conversational flow работает как `nutrition coach`: отвечает на свободные вопросы отдельно, не сохраняет их как factual entries, всегда опирается на актуальные метрики и recent entries текущего дня, держит короткую session memory около одного часа и поддерживает первый безопасный мультимодальный сценарий для фото продуктов или холодильника с явным coaching-caption.

Проект делается в первую очередь для личного использования, но архитектура должна позволять без существенных изменений дать доступ нескольким знакомым пользователям.

При этом доступ к самому боту и доступ к проектному LLM-провайдеру разделены:

- доступ к боту по-прежнему управляется через admin allow/deny;
- проектный `OpenAI`-ключ доступен только аккаунтам категории `internal`;
- пользователь может подключить собственные `OpenAI`- или `Mistral`-ключи через Telegram-команды;
- если персональный ключ недоступен или истёк, пользователь всё равно сохраняет доступ к своим данным и аналитике.

## Что доступно в первом релизе

- принимать текстовые сообщения о еде и воде;
- опционально принимать текстовые записи тренировок после явного включения этой функции в `/settings`;
- опционально принимать один скриншот тренировки и сохранять из него базовый workout-факт и factual calories тренировки, если они явно видны;
- принимать фото еды и использовать его как вход для оценки состава и параметров;
- выделять из сообщения отдельные блюда или позиции;
- оценивать количество, единицу измерения, КБЖУ и другие поддерживаемые метрики;
- учитывать включённые поддержанные диеты, сохранять item-level diet score для новых food entries через LLM и для water entries через фиксированное per-diet правило, а затем показывать quantity-weighted средний балл по диете в `/today` и `/report`;
- сохранять записи в PostgreSQL;
- возвращать пользователю краткий отчёт по добавленной записи и сводку по дню;
- отвечать на свободные nutrition-вопросы отдельно от journal flow;
- удалять запись целиком из списка последних записей;
- переключать источник LLM между project provider и персональными подключениями, если это допускает категория аккаунта.

## Основные сценарии

### Запись еды

Пользователь пишет сообщение вроде:

`съел паштет с хлебом и выпил кофе`

или отправляет фото блюда.

Система:

1. определяет, какие блюда или продукты были съедены;
2. оценивает объём, единицы измерения и пищевые показатели;
3. при включённой диете дополнительно сохраняет diet score по каждому блюду, а для воды использует заранее заданное высокое, но не максимальное правило без вызова LLM;
4. сохраняет запись в БД;
5. отправляет краткое подтверждение, обновлённый итог дня со средним diet score за день и delta этого среднего от новой записи, а также короткий diet-aware комментарий, если он уместен.

### Удаление записи

Пользователь может открыть `/recent`, выбрать одну из последних записей и удалить запись целиком. Для записей еды `/recent` также показывает средний diet score по записи. В `/report` бот отдельно показывает средний diet score за период, число оценённых позиций и число дней с оценкой. После удаления дневные показатели пересчитываются.

## Что не входит в первый релиз

- редактирование уже сохранённых записей;
- multi-image разбор скриншотов тренировок;
- mixed-media сценарии beyond текущего food photo / coaching photo contract.

## Импорт истории

Для начального переноса истории питания из Google Sheets проект поддерживает офлайн-импорт из CSV-экспорта листа.

Ограничения текущего шага:

- импортируется один CSV-лист, а не live-доступ к Google Sheets;
- одна строка CSV создаёт одну запись `Entry` и одну позицию `EntryItem`;
- если в строке есть только дата без времени, время восстанавливается детерминированно по типу приёма пищи;
- строки с водой импортируются как `water`, остальные строки как `food`;
- для затронутых дат сразу создаются `daily_goal_snapshots`, чтобы история не зависела от будущих изменений целей.

Пример запуска:

```bash
python -m food_registry_bot.importing.csv_import /path/to/nutrition.csv TELEGRAM_USER_ID
```

CSV должен содержать заголовки из текущей таблицы:

- `Дата`
- `Приём пищи`
- `Блюдо / продукт`
- `Категории`
- `Количество`
- `Единица`
- `Ккал`
- `Белки, г`
- `Жиры, г`
- `Углеводы, г`
- `Клетчатка, г`
- `Комментарий`
- `Источник оценки`
- `Уверенность оценки`

## Ключевые принципы

- Источник истины только один: PostgreSQL.
- Google Sheets не используются.
- Отчёты формируются в текстовом виде прямо в Telegram.
- Новые метрики добавляются разработчиком в коде, а не задаются пользователем произвольно.
- Пользователь может включать или отключать отслеживание поддержанных метрик в своём профиле.
- Поддержанные диеты задаются разработчиком через справочник и включаются пользователем отдельно от nutrition metric visibility.
- Функциональность тренировок включается пользователем отдельно и по умолчанию выключена.
- Исторические данные не должны изменять смысл при изменении текущих пользовательских настроек.
- Документация должна описывать реальные решения и не содержать процессных секций без практической пользы.

## Архитектурная схема

Систему предполагается строить из нескольких логических слоёв:

### Telegram layer

Отвечает за приём сообщений, фото и команд, а также за отправку пользователю итогового ответа.

### Message routing layer

Отвечает за безопасную границу между:

- command messages;
- journal messages;
- conversational messages;
- ambiguous messages, которые лучше отклонить без записи, чем сохранить неверный факт.

### Nutrition coach layer

Использует factual context дня пользователя и короткую session memory, чтобы:

- отвечать на nutrition / hydration / meal-planning вопросы и общие health topics;
- принимать фото холодильника или доступных продуктов как conversational input, если caption явно просит совет;
- добавлять короткий post-entry nutrition comment после успешной записи еды;
- не вмешиваться в routing и factual contract самого logging flow.

### AI parsing layer

Использует LLM для:

- извлечения структуры входного сообщения;
- оценки КБЖУ и других поддерживаемых метрик;
- подготовки краткого пользовательского комментария.

На текущем шаге для LLM действует такая политика:

- глобальный project provider: `OpenAI`;
- персональные providers: `OpenAI` и `Mistral`;
- доступность классов провайдеров задаётся конфигом;
- runtime выбирает project или personal key на уровне конкретного пользователя.

### Domain layer

Отвечает за нормализацию записей, пересчёт дневных итогов, применение пользовательских настроек и корректировку целей дня.

### Persistence layer

Хранит пользователей, записи, позиции записей, справочники метрик и диет, пользовательские diet preferences, значения метрик и дневные цели.

## Доменная модель

Ниже зафиксирована рабочая модель на уровне предметной области. Детали схемы БД и миграций следует уточнять в ADR.

### Пользователь

Хранит:

- Telegram identity;
- категорию LLM-доступа;
- часовой пояс;
- настройки отображения и отслеживания метрик;
- включённые диеты пользователя;
- базовые дневные цели;
- флаг включения workout logging;
- LLM profile и персональные provider connections.

### Запись

Запись - это событие в дневнике пользователя. В текущем состоянии поддерживаются записи еды, воды, text-first тренировок и базовый single-screenshot workout logging.

Примерные поля:

- пользователь;
- дата и время;
- тип записи;
- тип приёма пищи, если применимо;
- исходный текст сообщения;
- комментарий LLM;
- текущее состояние записи.

### Позиция записи

Одна запись может содержать несколько позиций. Например, в одном сообщении могут быть указаны паштет, хлеб и кофе.

Примерные поля:

- ссылка на запись;
- название блюда или продукта;
- количество;
- единица измерения;
- порядок внутри записи;
- уверенность оценки;
- источник оценки.

### Определение метрики

Справочник поддерживаемых системой метрик.

Примеры:

- `calories`
- `protein`
- `fat`
- `carbs`
- `fiber`
- `low_purine_score`
- `insulin_resistance_score`
- `gastritis_score`

Для метрики должны быть определены:

- код;
- тип значения;
- единица измерения;
- уровень применения: позиция, запись или день;
- системный статус;
- правила агрегации.

### Значение метрики

Хранит рассчитанное значение конкретной метрики для позиции, записи или дня.

Примерные поля:

- ссылка на определение метрики;
- ссылка на запись и/или позицию;
- числовое, текстовое или структурированное значение;
- комментарий или объяснение;
- источник оценки.

### Поддержанная диета

Developer-managed справочник диет, которые система умеет учитывать в write flow и аналитике.

Минимальные поля:

- стабильный код;
- display name;
- флаг системной доступности.

### Настройка диеты пользователя

Хранит независимое включение поддержанной диеты для конкретного пользователя.

Минимальные поля:

- ссылка на пользователя;
- ссылка на поддержанную диету;
- флаг включения.

### Дневные цели

Дневные цели должны храниться как снимок настроек на конкретную дату.

Это нужно для того, чтобы:

- прошлые дни не пересчитывались при изменении текущих настроек пользователя;
- исторические отчёты оставались интерпретируемыми.

Минимально здесь нужны:

- базовая цель дня;
- эффективная цель дня;
- дата;
- пользователь.

Например, если базовая цель пользователя составляет `1800` ккал, то при создании нового дня это значение фиксируется в дневной записи. Даже если текущие настройки потом изменятся, прошлые дни должны сохранить исходный смысл.

## Поддерживаемые метрики

На старте система должна уметь работать как минимум со следующими метриками:

- калории;
- белки;
- жиры;
- углеводы;
- клетчатка;
- объём воды;
- пуриновый риск.

Список поддерживаемых метрик расширяется разработчиком через код и миграции. Пользователь может выбрать, какие из поддерживаемых метрик показывать и отслеживать в своём контексте.

## Работа с LLM

LLM в проекте не сводится только к классификации текста. Она должна помогать в трёх связанных задачах:

1. извлечение структуры сообщения;
2. оценка нутриентных и дополнительных метрик;
3. ответа `nutrition coach` поверх factual context;
4. генерация краткого factual-ответа пользователю.

При этом система должна хранить разумные допущения, уровень уверенности и источник оценки, особенно в случаях с фото, сложными блюдами и неоднозначным описанием.

## Технологические ориентиры

- язык реализации: Python;
- бот запускается в Docker;
- основная база данных: PostgreSQL;
- развитие схемы БД должно идти через понятные инкрементальные миграции.

## Запуск на сервере через Docker

Для первого релиза проект можно поднимать на сервере через `docker compose`.

Что нужно подготовить:

- `BOT_TOKEN` - токен Telegram-бота;
- `ADMIN_USER_IDS` - список Telegram user id админов через запятую;
- `OPENAI_API_KEY` - ключ для extraction, nutrition estimation и conversational режима.

Проект не требует создавать `.env` в корне репозитория. Для Docker используется тот же sibling-файл, что и для локальной разработки:

`../food_registry_bot_local/.env`

Если файла ещё нет, создай его так:

```bash
./scripts/init-dev-secrets.sh
```

Минимальное содержимое `../food_registry_bot_local/.env` для сервера:

```env
APP_ENV=docker
BOT_TOKEN=
ADMIN_USER_IDS=
OPENAI_API_KEY=

POSTGRES_DB=food_registry
POSTGRES_USER=food_registry
POSTGRES_PASSWORD=food_registry

EXTRACTION_PROVIDER=llm
NUTRITION_PROVIDER=llm
LLM_MODEL=gpt-5-mini
NUTRITION_MODEL=gpt-5-mini
CONVERSATION_MODEL=gpt-5-mini
```

Для удобства есть wrapper-скрипт, который сам подставляет этот env-файл в `docker compose`.

Для релизного деплоя есть отдельный скрипт:

```bash
./scripts/release.sh
```

Он:

- проверяет наличие `../food_registry_bot_local/.env`;
- проверяет обязательные переменные `BOT_TOKEN`, `ADMIN_USER_IDS`, `OPENAI_API_KEY`;
- валидирует `docker compose` конфиг;
- выполняет `up --build -d`;
- показывает `ps` и последние логи бота.

Запуск:

```bash
./scripts/docker-compose.sh up --build -d
```

Что делает этот запуск:

- читает переменные из `../food_registry_bot_local/.env`;
- если запуск идёт из linked git worktree, автоматически добавляет к Docker project name и имени БД суффикс имени каталога дерева, а для опубликованного PostgreSQL-порта вычисляет отдельное значение, чтобы стеки разных деревьев не конфликтовали;
- если `APP_VERSION` не задан явно, автоматически подставляет текущий `git describe --tags --always --dirty`;
- поднимает PostgreSQL;
- собирает контейнер бота;
- ждёт готовности базы;
- выполняет `alembic upgrade head`;
- запускает polling-процесс бота.

Для бота также включён watchdog liveness-контроль:

- event loop периодически обновляет heartbeat-файл `var/run/bot-heartbeat`;
- container `healthcheck` проверяет свежесть этого heartbeat;
- если event loop зависнет и heartbeat перестанет обновляться, внутренний watchdog завершит процесс с кодом `1`, после чего `restart: unless-stopped` перезапустит контейнер.
- при следующем старте бот добавит в startup-уведомление админам признак нештатного рестарта и его причину, если она была зафиксирована.

Под это поведение доступны env-переменные:

- `WATCHDOG_ENABLED=true|false`
- `WATCHDOG_HEARTBEAT_INTERVAL_SECONDS=15`
- `WATCHDOG_TIMEOUT_SECONDS=90`

Для просмотра логов:

```bash
./scripts/docker-compose.sh logs -f bot
```

Для проверки состояния контейнеров:

```bash
./scripts/docker-compose.sh ps
```

Для остановки:

```bash
./scripts/docker-compose.sh down
```

Быстрая проверка после запуска:

1. `./scripts/docker-compose.sh ps` показывает `postgres` и `bot` в состоянии `running`.
2. В `./scripts/docker-compose.sh logs -f bot` нет ошибок `BOT_TOKEN is not configured`, `OPENAI_API_KEY is required` или ошибок миграций.
3. Бот отвечает на `/start`, открывает экран настроек и показывает постоянное меню; `/today` тоже работает.

Если нужно обновить приложение на сервере:

```bash
git pull
./scripts/release.sh
```

На этом шаге отдельный reverse proxy, отдельный worker-процесс и внешняя система мониторинга не требуются.

## Автодеплой по GitHub тегу

Для релизов принят формат тегов `vMAJOR.MINOR`:

- `v1.0`
- `v1.1`
- `v9.1`

При push нового тега такого вида GitHub Actions запускает workflow:

`/.github/workflows/release-on-tag.yml`

Workflow также можно запустить вручную через `Run workflow` и передать `git_ref`, например `main` или `v9.1`.

Что делает workflow:

- подключается к серверу по SSH;
- переходит в каталог проекта;
- делает `git fetch --tags origin`;
- делает `git checkout <tag>`;
- запускает `./scripts/release.sh`.

Нужные GitHub repository secrets:

- `DEPLOY_HOST` - адрес сервера;
- `DEPLOY_USER` - пользователь для деплоя;
- `DEPLOY_SSH_KEY` - приватный SSH-ключ для доступа к серверу;
- `DEPLOY_PROJECT_DIR` - абсолютный путь к проекту на сервере.

На текущем шаге workflow использует стандартный SSH-порт `22`.

Минимальный путь первого автодеплоя:

1. Развернуть проект на сервере вручную один раз.
2. Подготовить на сервере `../food_registry_bot_local/.env`.
3. Убедиться, что `./scripts/release.sh` работает на сервере вручную.
4. Добавить GitHub secrets.
5. Запустить workflow вручную с `git_ref=main` и убедиться, что деплой проходит.
6. После успешной ручной проверки запушить тег, например:

```bash
git tag v9.1
git push origin v9.1
```

## Локальная конфигурация

Секреты для локальной разработки не хранятся в репозитории и не добавляются в `.venv`.

Если нужно быстро восстановить то же Python-окружение в другом worktree или соседнем дереве репозитория, используй:

```bash
python -m venv .venv
. .venv/bin/activate
pip install -r requirements.txt
```

Файл `requirements.txt` фиксирует полный набор библиотек из рабочего окружения, включая dev-инструменты и транзитивные зависимости.

Проект ожидает локальный файл окружения по пути:

`../food_registry_bot_local/.env`

относительно корня репозитория.

Для создания каталога и шаблона файла используй:

```bash
./scripts/init-dev-secrets.sh
```

Скрипт создаёт sibling-каталог `food_registry_bot_local`, добавляет в него `.env` с болванкой переменных и `README.md` с пояснением назначения каталога.

Для admin-команд укажи в локальном `.env` переменную:

```env
ADMIN_USER_IDS=123456789
```

Если админов несколько, перечисли их через запятую.

Минимальный шаблон локального `.env` на текущем этапе выглядит так:

```env
APP_ENV=local
BOT_TOKEN=
ADMIN_USER_IDS=
EXTRACTION_PROVIDER=structured_payload
LLM_MODEL=gpt-5-mini
NUTRITION_MODEL=gpt-5-mini
CONVERSATION_MODEL=gpt-5-mini
OPENAI_API_KEY=

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=food_registry
POSTGRES_USER=food_registry
POSTGRES_PASSWORD=food_registry
```

Для extraction layer на текущем этапе поддерживаются два режима:

- `EXTRACTION_PROVIDER=structured_payload` - дев-режим, где бот принимает уже нормализованный structured payload;
- `EXTRACTION_PROVIDER=llm` - режим для прямой LLM-интеграции через OpenAI Responses API.

Если включён `EXTRACTION_PROVIDER=llm`, приложение использует `OPENAI_API_KEY` и `LLM_MODEL` для создания OpenAI adapter. Сам Telegram handler при этом не меняется: он по-прежнему работает только с extraction service boundary.

На текущем этапе direct LLM extraction поддерживает:

- text-only сообщения;
- одно фото еды с опциональным caption;
- photo album или media group не поддерживаются: бот просит прислать одно основное фото;
- тот же `entries` extraction contract на выходе, что и для structured payload режима.

Для conversational `nutrition coach` на текущем этапе отдельный provider-флаг не нужен:

- если `OPENAI_API_KEY` не задан, conversational routing остаётся доступным, но вместо LLM-ответа бот явно сообщает, что `nutrition coach` не настроен;
- если `OPENAI_API_KEY` задан, бот использует OpenAI Responses API для factual-aware coach reply.

Модели на текущем этапе разделены так:

- `LLM_MODEL` - модель extraction layer;
- `NUTRITION_MODEL` - модель nutrition estimation layer;
- `CONVERSATION_MODEL` - модель `nutrition coach`.

Если не нужен отдельный тюнинг, все три можно оставить одинаковыми.

## Документация

- `README.md` описывает проект на верхнем уровне;
- `docs/user-functions.md` фиксирует пользовательские сценарии и текущий наблюдаемый результат для пользователя;
- `docs/development-workflow.md` фиксирует рабочий процесс и короткий путь релиза;
- архитектурные и модельные решения фиксируются в `docs/adr/`;
- `AGENTS.md` хранит постоянные инструкции по работе с репозиторием.
